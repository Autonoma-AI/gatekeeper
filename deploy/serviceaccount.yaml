# ServiceAccount the Gatekeeper pod runs as. No IRSA annotation: it talks to the
# in-cluster API server, not AWS.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gatekeeper
  namespace: preview-example

# Binaries
/bin/
/out/
# Anchored to the repo root so it only ignores a root-level `gatekeeper` binary
# (e.g. from `go build` at the root) - NOT the cmd/gatekeeper/ source package.
/gatekeeper

# Test / coverage
*.out
coverage.txt

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
